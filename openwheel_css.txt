===== static/css/base.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Base e Layout (Shell)
   ============================================================ */

* {
    box-sizing: border-box;
}

html, body {
    margin: 0;
    padding: 0;
    font-family: var(--font-family);
    background: var(--bg-body);
    color: var(--text-main);
    min-height: 100vh;
}

a {
    color: inherit;
    text-decoration: none;
}

h1, h2, h3, h4 {
    margin: 0 0 12px 0;
    letter-spacing: 0.5px;
}

p {
    margin: 0 0 12px 0;
    color: var(--text-muted);
}

hr {
    border: none;
    border-top: 1px solid var(--border-main);
    margin: var(--spacing-md) 0;
}

/* --------------------------------------
   SHELL LOGADO (Layout Principal)
--------------------------------------- */
.ows-shell {
    display: flex;
    min-height: 100vh;
}

/* SIDEBAR */
.ows-sidebar {
    width: 260px;
    background: var(--bg-sidebar);
    border-right: 1px solid var(--border-dark);
    padding: 20px 0 40px 0;
    position: sticky;
    top: 0;
    height: 100vh;
    overflow-y: auto;
}

.ows-logo {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 0 20px 24px 20px;
    border-bottom: 1px solid var(--border-dark);
    margin-bottom: var(--spacing-md);
}

.ows-logo-mark {
    width: 42px;
    height: 42px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: 22px;
    color: var(--text-white);
    box-shadow: var(--shadow-logo);
}

.ows-logo-text {
    font-weight: 800;
    font-size: 14px;
    line-height: 1.1;
    letter-spacing: 2px;
}

.ows-logo-text span {
    color: var(--color-primary-light);
    font-weight: 600;
    letter-spacing: 3px;
}

.ows-menu-section {
    padding: 18px 20px 8px 20px;
    font-size: 11px;
    letter-spacing: 2px;
    color: var(--text-dark);
    font-weight: 700;
}

.ows-menu-link {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 20px;
    color: var(--text-muted);
    font-size: 14px;
    transition: all .15s ease;
    border-left: 3px solid transparent;
}

.ows-menu-link:hover {
    background: var(--bg-hover);
    color: var(--text-white);
}

.ows-menu-link.active {
    background: linear-gradient(90deg, var(--color-primary-faded), transparent);
    color: var(--text-white);
    border-left-color: var(--color-primary);
    font-weight: 600;
}

.ows-icon {
    width: 22px;
    text-align: center;
}

/* ÁREA PRINCIPAL */
.ows-main {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
}

.ows-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 32px;
    background: var(--bg-topbar);
    border-bottom: 1px solid var(--border-dark);
    position: sticky;
    top: 0;
    z-index: 20;
}

.ows-topbar-title {
    font-size: 12px;
    letter-spacing: 4px;
    color: var(--color-primary-light);
    font-weight: 700;
}

.ows-topbar-right {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
}

.ows-content {
    padding: var(--spacing-xl);
    flex: 1;
}

/* --------------------------------------
   LAYOUT VISITANTE (Telas sem Login)
--------------------------------------- */
.ows-guest {
    min-height: 100vh;
    background: var(--bg-body);
    display: flex;
    flex-direction: column;
}

.ows-guest-topbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 18px 30px;
    border-bottom: 1px solid var(--border-dark);
    background: var(--bg-topbar);
}

.ows-guest-brand {
    font-weight: 800;
    letter-spacing: 2px;
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--text-white);
}

.ows-guest-content {
    padding: 40px;
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
    flex: 1;
}
===== static/css/componentes.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Componentes Reutilizáveis
   ============================================================ */

/* --------------------------------------
   GRIDS
--------------------------------------- */
.ows-grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: var(--spacing-md); }
.ows-grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--spacing-md); }
.ows-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: var(--spacing-md); }
@media (max-width: 1200px) { .ows-grid-4 { grid-template-columns: repeat(2, 1fr); } .ows-grid-3 { grid-template-columns: 1fr 1fr; } }
@media (max-width: 800px) { .ows-grid-4, .ows-grid-3, .ows-grid-2 { grid-template-columns: 1fr; } }

/* --------------------------------------
   CARDS
--------------------------------------- */
.ows-card { background: linear-gradient(180deg, var(--bg-card-start), var(--bg-card-end)); border: 1px solid var(--border-main); border-radius: var(--radius-lg); padding: 22px; box-shadow: var(--shadow-card); }
.ows-card h3 { font-size: 11px; letter-spacing: 3px; color: var(--text-label); font-weight: 700; margin-bottom: 12px; }
.ows-card .ows-value { font-size: 32px; font-weight: 700; color: var(--text-white); letter-spacing: 1px; }
.ows-card .ows-hint { font-size: 12px; color: var(--text-label); margin-top: 6px; }

/* --------------------------------------
   HERO SECTIONS
--------------------------------------- */
.ows-hero { position: relative; border-radius: var(--radius-xl); padding: 34px; margin-bottom: var(--spacing-lg); overflow: hidden; background: radial-gradient(circle at 90% 20%, var(--color-primary-faded), transparent 60%), linear-gradient(135deg, var(--bg-surface) 0%, var(--bg-body) 100%); border: 1px solid var(--border-light); }
.ows-hero-label { display: inline-block; padding: 5px 12px; background: var(--color-primary-faded); border: 1px solid var(--color-primary-border); color: var(--color-primary-light); border-radius: 999px; font-size: 11px; letter-spacing: 2px; font-weight: 700; margin-bottom: 14px; }
.ows-hero h1 { font-size: 30px; font-weight: 800; letter-spacing: 1px; color: var(--text-white); margin-bottom: 6px; }
.ows-hero .ows-hero-sub { color: var(--text-muted); margin-bottom: 18px; font-size: 14px; }
.ows-hero-meta { display: flex; gap: 30px; flex-wrap: wrap; margin-top: 20px; }
.ows-hero-meta > div { line-height: 1.2; }
.ows-hero-meta small { color: var(--text-label); font-size: 11px; letter-spacing: 2px; font-weight: 700; }
.ows-hero-meta strong { display: block; font-size: 18px; font-weight: 700; color: var(--text-white); margin-top: 4px; }
.ows-hero-actions { margin-top: 26px; display: flex; gap: 12px; flex-wrap: wrap; }

/* --------------------------------------
   BOTÕES
--------------------------------------- */
.ows-btn { display: inline-block; padding: 10px 18px; border-radius: var(--radius-md); font-weight: 600; font-size: 14px; cursor: pointer; border: 1px solid transparent; transition: all .15s ease; text-align: center; }
.ows-btn:disabled { opacity: 0.5; cursor: not-allowed; }
.ows-btn-primary { background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark)); color: var(--text-white); box-shadow: var(--shadow-btn-primary); }
.ows-btn-primary:hover:not(:disabled) { filter: brightness(1.05); }
.ows-btn-ghost { background: transparent; color: var(--text-main); border-color: var(--border-light); }
.ows-btn-ghost:hover:not(:disabled) { background: var(--bg-surface); color: var(--text-white); }
.ows-btn-danger { background: var(--color-danger); color: var(--text-white); }
.ows-btn-full { display: block; width: 100%; }

/* --------------------------------------
   BADGES & ALERTAS
--------------------------------------- */
.ows-badge { background: var(--bg-surface); border: 1px solid var(--border-main); border-radius: var(--radius-md); padding: 6px 14px; line-height: 1.1; }
.ows-badge-label { font-size: 10px; letter-spacing: 2px; color: var(--text-label); font-weight: 700; }
.ows-badge-value { font-size: 13px; font-weight: 600; color: var(--text-white); }
.ows-alert { padding: 12px 16px; border-radius: var(--radius-md); font-size: 13px; margin-bottom: var(--spacing-md); }
.ows-alert-danger { background: var(--color-danger-bg); border: 1px solid var(--color-danger-border); color: var(--color-danger-light); }
.ows-alert-success { background: var(--color-success-bg); border: 1px solid var(--color-success-border); color: var(--color-success-light); }
.ows-alert-warning { background: var(--color-warning-bg); border: 1px solid var(--color-warning-border); color: var(--color-warning); }
.ows-alert-info { background: var(--color-info-bg); border: 1px solid var(--color-info-border); color: var(--color-info); }

/* --------------------------------------
   UTILITÁRIOS GLOBAIS (Cores & Espaçamento)
--------------------------------------- */
.ows-muted { color: var(--text-label); }
.ows-strong { color: var(--text-white); font-weight: 600; }
.ows-orange { color: var(--color-primary-light); }
.ows-text-danger { color: var(--color-danger) !important; }
.ows-text-success { color: var(--color-success) !important; }
.ows-text-warning { color: var(--color-warning) !important; }
.ows-text-info { color: var(--color-info) !important; }
.ows-text-primary { color: var(--color-primary) !important; }

.ows-m-0 { margin: 0 !important; }
.ows-mt-xs { margin-top: 4px !important; }
.ows-mt-sm { margin-top: var(--spacing-sm) !important; }
.ows-mt-md { margin-top: var(--spacing-md) !important; }
.ows-mt-lg { margin-top: var(--spacing-lg) !important; }
.ows-mb-xs { margin-bottom: 4px !important; }
.ows-mb-sm { margin-bottom: var(--spacing-sm) !important; }
.ows-mb-md { margin-bottom: var(--spacing-md) !important; }
.ows-mb-lg { margin-bottom: var(--spacing-lg) !important; }
===== static/css/formularios.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Formulários
   ============================================================ */

.ows-form {
    display: flex;
    flex-direction: column;
}

.ows-form label,
label.ows-label {
    font-size: 11px;
    letter-spacing: 2px;
    color: var(--text-label);
    font-weight: 700;
    margin-top: 12px;
    margin-bottom: 6px;
    display: block;
}

.ows-form input[type="text"],
.ows-form input[type="email"],
.ows-form input[type="password"],
.ows-form input[type="number"],
.ows-form select,
input.ows-input,
select.ows-select {
    width: 100%;
    background: var(--bg-sidebar);
    border: 1px solid var(--border-light);
    color: var(--text-white);
    padding: 12px 14px;
    border-radius: var(--radius-md);
    font-size: 14px;
    outline: none;
    font-family: var(--font-family);
    transition: border-color 0.15s ease;
}

.ows-form input:focus,
.ows-form select:focus,
input.ows-input:focus,
select.ows-select:focus {
    border-color: var(--color-primary);
}

.ows-form input:disabled,
.ows-form select:disabled,
input.ows-input:disabled,
select.ows-select:disabled {
    opacity: 0.6;
    cursor: not-allowed;
    background: var(--bg-body);
}

.ows-form select option {
    background: var(--bg-sidebar);
    color: var(--text-white);
}

.ows-form button[type="submit"] {
    margin-top: 20px;
}

/* Inputs de Range (Sliders Customizados) */
input[type=range].ows-range {
    -webkit-appearance: none;
    width: 100%;
    background: transparent;
}

input[type=range].ows-range::-webkit-slider-thumb {
    -webkit-appearance: none;
    height: 18px;
    width: 18px;
    border-radius: 50%;
    background: var(--color-primary);
    cursor: pointer;
    margin-top: -6px;
}

input[type=range].ows-range::-webkit-slider-runnable-track {
    width: 100%;
    height: 6px;
    cursor: pointer;
    background: var(--border-light);
    border-radius: 3px;
}

/* --------------------------------------
   Telas de Autenticação e Landing Puras
--------------------------------------- */
.ows-landing {
    max-width: 720px;
    margin: 40px auto;
    text-align: center;
    background: linear-gradient(180deg, var(--bg-card-start), var(--bg-card-end));
    border: 1px solid var(--border-main);
    border-radius: var(--radius-xl);
    padding: 50px 40px;
}

.ows-landing h1 {
    font-size: 30px;
    color: var(--text-white);
    letter-spacing: 1px;
}

.ows-landing-sub {
    color: var(--text-muted);
    font-size: 15px;
    margin: 12px 0 26px;
}

.ows-auth-wrap {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    padding: 30px 0;
}

.ows-auth-card {
    width: 100%;
    max-width: 420px;
    background: linear-gradient(180deg, var(--bg-card-start), var(--bg-card-end));
    border: 1px solid var(--border-main);
    border-radius: var(--radius-xl);
    padding: 34px;
}

.ows-auth-card h1 {
    font-size: 24px;
    color: var(--text-white);
    margin: 8px 0 4px;
    letter-spacing: 1px;
}

.ows-divider {
    display: flex;
    align-items: center;
    gap: 10px;
    margin: 20px 0;
    color: var(--text-dark);
    font-size: 12px;
}

.ows-divider::before, .ows-divider::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--border-main);
}

.ows-auth-footer {
    text-align: center;
    margin-top: 20px;
    font-size: 13px;
    color: var(--text-muted);
}
===== static/css/telas/admin.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Telas: Admin Global
   ============================================================ */

/* Container principal agora empilha verticalmente (Top Menu + Conteúdo) */
.ows-admin-container {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
}

/* Menu de Navegação Horizontal */
.ows-admin-nav {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    background: var(--bg-card-start);
    padding: 16px;
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-main);
    box-shadow: var(--shadow-card);
    align-items: center;
}

.ows-admin-nav-item {
    padding: 8px 16px;
    background: var(--bg-surface);
    color: var(--text-muted);
    border: 1px solid var(--border-main);
    border-radius: var(--radius-md);
    transition: all 0.15s ease;
    font-size: 13px;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 6px;
}

.ows-admin-nav-item:hover {
    background: var(--bg-hover);
    color: var(--text-white);
    border-color: var(--border-light);
}

.ows-admin-nav-item.active {
    background: var(--color-primary-faded);
    border-color: var(--color-primary-border);
    color: var(--color-primary-light);
}

/* Separador visual entre áreas do menu */
.ows-admin-nav-divider {
    width: 1px;
    height: 24px;
    background: var(--border-light);
    margin: 0 4px;
}

/* Rótulo de texto para seções (ex: Fornecedores) */
.ows-admin-nav-label {
    padding: 0 8px;
    color: var(--text-label);
    font-weight: 800;
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
}

/* O conteúdo do Admin */
.ows-admin-content {
    width: 100%;
}

/* Tabelas e Forms (Mantidos) */
.ows-admin-table { 
    width: 100%; 
    border-collapse: collapse; 
    text-align: left; 
    background: var(--bg-card-start); 
    border-radius: var(--radius-md); 
    overflow: hidden; 
    margin-bottom: var(--spacing-md); 
}
.ows-admin-table th { 
    padding: 12px; 
    border-bottom: 2px solid var(--border-dark); 
    background: var(--bg-surface); 
    color: var(--text-label); 
    font-size: 12px; 
    letter-spacing: 1px; 
    text-transform: uppercase; 
}
.ows-admin-table td { 
    padding: 12px; 
    border-bottom: 1px solid var(--border-main); 
    color: var(--text-main); 
    font-size: 14px; 
}
.ows-admin-table tr:hover td { 
    background: var(--bg-hover); 
}
.ows-admin-table-danger td { 
    color: var(--color-danger-light); 
    background: var(--color-danger-bg); 
}
.ows-admin-inline-form { 
    display: inline-block; 
}
.admin-input-sm { 
    width: 100px; 
}
.admin-input-md { 
    width: 120px; 
}
===== static/css/telas/classificacao.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tela: Classificação
   ============================================================ */

.classi-title {
    margin-bottom: var(--spacing-md);
    font-size: 24px;
    color: var(--text-white);
}

.classi-subtitle {
    margin-top: var(--spacing-lg);
    margin-bottom: var(--spacing-md);
    font-size: 20px;
    color: var(--text-white);
}

.classi-table {
    width: 100%;
    border-collapse: collapse;
    text-align: left;
    background: var(--bg-card-start);
    border-radius: var(--radius-md);
    overflow: hidden;
}

.classi-table th {
    padding: 12px 16px;
    border-bottom: 2px solid var(--border-dark);
    background: var(--bg-surface);
    color: var(--text-label);
    font-size: 12px;
    letter-spacing: 1px;
    text-transform: uppercase;
}

.classi-table td {
    padding: 12px 16px;
    border-bottom: 1px solid var(--border-main);
    color: var(--text-main);
    font-size: 14px;
}

.classi-empty-msg {
    margin-top: 15px;
}
===== static/css/telas/corrida.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tela: Corrida (Replay)
   ============================================================ */

.corrida-title {
    font-size: 24px;
    color: var(--text-white);
}

.corrida-subtitle {
    font-size: 20px;
    color: var(--text-white);
}

.corrida-controls {
    display: flex;
    gap: var(--spacing-sm);
    flex-wrap: wrap;
}

.corrida-table {
    width: 100%;
    border-collapse: collapse;
    text-align: left;
}

.corrida-table th {
    padding: 10px;
    border-bottom: 2px solid var(--border-dark);
    color: var(--text-label);
    font-size: 12px;
    letter-spacing: 1px;
    text-transform: uppercase;
}

.corrida-table td {
    padding: 12px 10px;
    border-bottom: 1px solid var(--border-main);
    color: var(--text-main);
    font-size: 14px;
}

.corrida-row-danger td {
    background: var(--color-danger-bg);
    color: var(--color-danger-light);
}
===== static/css/telas/desenvolvimento.css =====
.desenv-card-info { background: var(--color-primary-faded); }
.desenv-card-danger { border-left: 3px solid var(--color-danger); }
.desenv-title-danger { color: var(--color-danger-light); }
.desenv-value-lg { font-size: 42px; }
.desenv-cost-label { font-size: 12px; }
.desenv-cost-value { font-size: 18px; font-weight: 600; color: var(--text-white); }
.desenv-req-item { display: flex; gap: 12px; align-items: center; padding: 8px 0; }
.desenv-icon-success { color: var(--color-success); font-size: 20px; }
.desenv-icon-danger { color: var(--color-danger); font-size: 20px; }
.desenv-current-value { font-size: 20px; font-weight: 600; color: var(--text-white); }
.desenv-btn-text { text-align: center; }
===== static/css/telas/equipes.css =====
.eq-form-block { display: block; }
.eq-card-highlight { border-left: 3px solid var(--color-primary); }
.eq-title-primary { color: var(--color-primary-light); }
.eq-card-warning { background: var(--color-primary-faded); }
.eq-btn-submit { font-size: 15px; padding: 14px 32px; }
===== static/css/telas/estrategia_corrida.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tela: Estratégia
   ============================================================ */

.estrategia-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--spacing-md);
}

.estrategia-header h1 {
    margin-bottom: 0;
}

.pit-wall-container {
    display: flex;
    gap: var(--spacing-md);
    align-items: flex-start;
    margin-top: var(--spacing-md);
}

.strategy-editor {
    flex: 2;
    background: var(--bg-surface);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    box-shadow: var(--shadow-card);
}

.pit-wall-summary {
    flex: 1;
    background: var(--bg-card-start);
    border: 1px solid var(--border-main);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    position: sticky;
    top: var(--spacing-lg);
    font-family: var(--font-mono);
}

.pit-wall-summary h3 {
    color: var(--text-label);
    font-size: 14px;
    letter-spacing: 2px;
    margin-bottom: 20px;
    border-bottom: 1px solid var(--border-dark);
    padding-bottom: 10px;
}

.summary-item {
    display: flex;
    justify-content: space-between;
    margin-bottom: 15px;
    font-size: 16px;
}

.summary-item span:first-child {
    color: var(--text-muted);
}

.summary-item strong {
    color: var(--text-white);
}

.timeline-bar {
    display: flex;
    height: 30px;
    background: var(--bg-body);
    border-radius: var(--radius-sm);
    overflow: hidden;
    margin-bottom: 25px;
}

.timeline-stint {
    display: flex;
    align-items: center;
    justify-content: center;
    color: #000;
    font-weight: 700;
    font-size: 12px;
    font-family: var(--font-mono);
    transition: width 0.3s;
}

.pneu-seco {
    background-color: var(--color-warning);
}

.pneu-molhado {
    background-color: var(--color-success);
}

.pneu-encharcado {
    background-color: var(--color-info);
}

.stint-card {
    background: var(--bg-card-end);
    /* Borda inicial base; será sobrescrita pelo JS dependendo do pneu */
    border-left: 4px solid var(--color-warning); 
    padding: 15px;
    margin-bottom: 15px;
    border-radius: var(--radius-sm);
    display: flex;
    gap: 15px;
    align-items: center;
}

.btn-remover-stint {
    background: var(--color-danger);
    color: var(--text-white);
    border: none;
    padding: 8px 12px;
    border-radius: var(--radius-sm);
    cursor: pointer;
    font-weight: 700;
}

.btn-add-stint {
    background: var(--bg-sidebar);
    color: var(--text-white);
    border: 1px dashed var(--text-label);
    width: 100%;
    padding: 12px;
    text-align: center;
    cursor: pointer;
    border-radius: var(--radius-sm);
    margin-bottom: 20px;
}

.btn-add-stint:hover:not(:disabled) {
    background: var(--bg-hover);
}

.btn-add-stint:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.estrategia-btn-save {
    padding: 15px;
    font-weight: 700;
    text-transform: uppercase;
    margin-top: 15px;
}

.estrategia-title {
    color: var(--text-white);
    margin-bottom: 20px;
}

.estrategia-stint-col-flex {
    flex: 1;
}

.estrategia-stint-label {
    color: var(--text-muted);
    font-size: 12px;
    display: block;
    margin-bottom: 5px;
}

.estrategia-select-full {
    width: 100%;
}

.estrategia-input-sm {
    width: 80px;
}

.estrategia-input-md {
    width: 100px;
}

.estrategia-col-end {
    align-self: flex-end;
}

.estrategia-validation-msg {
    font-weight: 700;
    margin-bottom: 15px;
    font-size: 14px;
}

.estrategia-fase-b {
    color: var(--text-label);
}

/* Classes dinâmicas injetadas via JS para a cor da borda do card */
.stint-border-seco { border-left-color: var(--color-warning) !important; }
.stint-border-molhado { border-left-color: var(--color-success) !important; }
.stint-border-encharcado { border-left-color: var(--color-info) !important; }
===== static/css/telas/minha_equipe.css =====
.mq-section-title { font-size: 14px; letter-spacing: 3px; color: var(--text-label); }
.mq-value-md { font-size: 20px; font-weight: 700; color: var(--text-white); }
.mq-text-disabled { font-size: 20px; color: var(--text-dark); }
.mq-card-highlight { border-left: 3px solid var(--color-primary); }
.mq-title-primary { color: var(--color-primary-light); }
===== static/css/telas/pistas_reais.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tela: Pistas Reais (Galeria SVG)
   ============================================================ */
.pistas-reais-card { display: flex; gap: var(--spacing-lg); align-items: center; }
.pista-svg { flex-shrink: 0; }
.pista-titulo { color: var(--color-primary-light); font-size: 20px; margin-bottom: var(--spacing-xs); }
.pista-meta { color: var(--text-muted); font-size: 14px; margin-bottom: 4px; }
.pista-legenda { color: var(--text-label); font-size: 12px; margin-top: var(--spacing-lg); text-align: center; }
===== static/css/telas/ranking.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tela: Ranking de Treino Livre
   ============================================================ */
.ranking-header { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: var(--spacing-md); margin-bottom: var(--spacing-xs); }
.ranking-title { margin: 0; color: var(--text-white); font-size: 24px; }
.ranking-subtitle { color: var(--text-muted); margin-bottom: var(--spacing-lg); }
.ranking-row-me td { background: var(--color-primary-faded); }
===== static/css/telas/temporada.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tela: Temporada
   ============================================================ */

.temp-title {
    margin-bottom: var(--spacing-lg);
    font-size: 24px;
    color: var(--text-white);
}

.temp-code {
    background: var(--bg-body);
    padding: 2px 6px;
    border-radius: var(--radius-sm);
    font-family: var(--font-mono);
    color: var(--color-info);
}

.temp-mb {
    margin-bottom: var(--spacing-lg);
}

.temp-card-title {
    margin-bottom: var(--spacing-sm);
    font-size: 20px;
    color: var(--color-primary-light);
}

.temp-subtitle {
    margin-top: var(--spacing-lg);
    margin-bottom: var(--spacing-md);
    font-size: 16px;
    color: var(--text-white);
}

.temp-table {
    width: 100%;
    border-collapse: collapse;
    text-align: left;
    background: var(--bg-card-start);
    border-radius: var(--radius-md);
    overflow: hidden;
}

.temp-table th {
    padding: 10px 16px;
    border-bottom: 2px solid var(--border-dark);
    background: var(--bg-surface);
    color: var(--text-label);
    font-size: 12px;
    letter-spacing: 1px;
    text-transform: uppercase;
}

.temp-table td {
    padding: 12px 16px;
    border-bottom: 1px solid var(--border-main);
    color: var(--text-main);
    font-size: 14px;
}
===== static/css/telas/treinamento.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tela: Treinamento
   ============================================================ */

.trein-title {
    margin-bottom: var(--spacing-md);
    font-size: 24px;
    color: var(--text-white);
}

.trein-subtitle {
    margin-bottom: var(--spacing-md);
}

.trein-progress-wrapper {
    position: relative;
    width: 100%;
    height: 24px;
    margin-bottom: var(--spacing-md);
    background: var(--bg-body);
    border-radius: var(--radius-sm);
    overflow: hidden;
}

.trein-progress {
    width: 100%;
    height: 100%;
    -webkit-appearance: none;
    appearance: none;
    border: none;
    background: transparent;
}

.trein-progress::-webkit-progress-bar {
    background-color: transparent;
}

.trein-progress::-webkit-progress-value {
    background-color: var(--color-danger);
}

.trein-progress::-moz-progress-bar {
    background-color: var(--color-danger);
}

.trein-progress-text {
    position: absolute;
    top: 0; 
    left: 0; 
    right: 0; 
    bottom: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    font-weight: 700;
    color: var(--text-white);
}

.trein-status-text {
    margin-bottom: var(--spacing-md);
}
===== static/css/telas/treino_livre.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tela: Treino Livre
   ============================================================ */

.treino-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-md);
}

.treino-header h1 {
    margin-bottom: 0;
}

.treino-grid-start {
    align-items: flex-start;
}

.treino-card-mb {
    margin-bottom: var(--spacing-lg);
}

.treino-subtitle {
    margin-top: 4px;
}

.treino-grid-mt {
    margin-top: var(--spacing-md);
}

.treino-label {
    letter-spacing: 1px;
}

.treino-value-sm {
    font-size: 18px;
    color: var(--text-white);
    font-weight: 700;
    margin-top: 4px;
}

.treino-input-mb {
    margin-bottom: 12px;
}

.treino-btn-lg {
    padding: 15px;
    font-size: 16px;
    font-weight: 700;
}

/* Nova classe para substituir o style inline do botão "Fazer Volta" */
.treino-btn-volta {
    margin-bottom: var(--spacing-md);
}

.treino-telemetry-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--spacing-md);
}

.treino-telemetry-title {
    margin-bottom: 4px;
    color: var(--color-warning);
}

.treino-telemetry-subtitle {
    margin: 0;
}

.treino-telemetry-stats {
    display: flex;
    gap: 20px;
    text-align: right;
}

.treino-stat-val {
    font-size: 20px;
    font-weight: 700;
    color: var(--text-white);
}

.treino-acerto-box {
    margin: 20px 0;
    padding: 15px;
    background: var(--color-success-bg);
    border-left: 4px solid var(--color-success);
    border-radius: var(--radius-sm);
}

.treino-acerto-label {
    display: block;
    margin-bottom: 5px;
    font-weight: 700;
}

.treino-acerto-val {
    font-size: 32px;
    font-weight: 700;
    color: var(--color-success);
}

.treino-slider-row {
    margin-bottom: 20px;
}

.treino-slider-label {
    display: flex;
    justify-content: space-between;
    color: var(--text-white);
    font-family: var(--font-mono);
    margin-bottom: var(--spacing-sm);
    font-size: 14px;
}

.treino-slider-row-last {
    margin-bottom: 30px;
}

.treino-alert-fatal {
    background: var(--color-danger);
    color: var(--text-white);
    border: none;
    font-weight: 700;
    text-align: center;
}

.treino-actions-row {
    display: flex;
    gap: var(--spacing-sm);
}

.treino-actions-row .ows-btn {
    flex: 1;
}

.treino-btn-success {
    color: var(--color-success);
    border-color: var(--color-success);
}

.treino-btn-success:hover {
    background: var(--color-success-bg);
    color: var(--text-white);
}

.treino-btn-danger {
    color: var(--color-danger);
    border-color: var(--color-danger);
}

.treino-btn-danger:hover {
    background: var(--color-danger-bg);
    color: var(--text-white);
}

.treino-table-wrapper {
    overflow-x: auto;
    margin-top: 10px;
}

.treino-table {
    width: 100%;
    border-collapse: collapse;
}

.treino-table thead tr {
    border-bottom: 2px solid var(--border-light);
}

.treino-table th {
    padding: 8px;
    color: var(--text-label);
    text-align: left;
    font-weight: 600;
    font-size: 13px;
}

.treino-table tbody tr {
    border-bottom: 1px solid var(--border-dark);
}

.treino-table td {
    padding: 12px 8px;
    font-size: 14px;
}

.treino-td-bold {
    font-weight: 700;
    color: var(--text-white);
}

.treino-td-success {
    font-weight: 700;
    color: var(--color-success);
}

.treino-td-center {
    text-align: center;
    padding: 20px;
}
===== static/css/tema.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Tema visual v4.1
   Substitui totalmente o tema.css antigo.
   ============================================================ */

* { box-sizing: border-box; }
html, body {
    margin: 0; padding: 0;
    font-family: 'Segoe UI', 'Inter', Tahoma, sans-serif;
    background: #08101e;
    color: #eef2f8;
    min-height: 100vh;
}
a { color: inherit; text-decoration: none; }
h1, h2, h3, h4 { margin: 0 0 12px 0; letter-spacing: 0.5px; }
p  { margin: 0 0 12px 0; color: #b2bccb; }
hr { border: none; border-top: 1px solid #1e2a3d; margin: 16px 0; }

/* SHELL LOGADO */
.ows-shell { display: flex; min-height: 100vh; }

/* SIDEBAR */
.ows-sidebar {
    width: 260px; background: #0b1524; border-right: 1px solid #1a2740;
    padding: 20px 0 40px 0; position: sticky; top: 0;
    height: 100vh; overflow-y: auto;
}
.ows-logo {
    display: flex; align-items: center; gap: 12px;
    padding: 0 20px 24px 20px; border-bottom: 1px solid #1a2740; margin-bottom: 16px;
}
.ows-logo-mark {
    width: 42px; height: 42px; border-radius: 50%;
    background: linear-gradient(135deg, #ff7a00, #ff3d00);
    display: flex; align-items: center; justify-content: center;
    font-weight: 800; font-size: 22px; color: #fff;
    box-shadow: 0 0 20px rgba(255,120,0,.35);
}
.ows-logo-text { font-weight: 800; font-size: 14px; line-height: 1.1; letter-spacing: 2px; }
.ows-logo-text span { color: #ff8a3d; font-weight: 600; letter-spacing: 3px; }

.ows-menu-section {
    padding: 18px 20px 8px 20px; font-size: 11px; letter-spacing: 2px;
    color: #5a6a83; font-weight: 700;
}
.ows-menu-link {
    display: flex; align-items: center; gap: 10px;
    padding: 10px 20px; color: #b6c1d3; font-size: 14px;
    transition: all .15s ease; border-left: 3px solid transparent;
}
.ows-menu-link:hover { background: #0f1c30; color: #fff; }
.ows-menu-link.active {
    background: linear-gradient(90deg, rgba(255,122,0,.12), transparent);
    color: #fff; border-left-color: #ff7a00; font-weight: 600;
}
.ows-icon { width: 22px; text-align: center; }

/* ÁREA PRINCIPAL */
.ows-main { flex: 1; min-width: 0; display: flex; flex-direction: column; }

.ows-topbar {
    display: flex; align-items: center; justify-content: space-between;
    padding: 18px 32px; background: #0a1322;
    border-bottom: 1px solid #1a2740;
    position: sticky; top: 0; z-index: 20;
}
.ows-topbar-title { font-size: 12px; letter-spacing: 4px; color: #ff8a3d; font-weight: 700; }
.ows-topbar-right { display: flex; align-items: center; gap: 16px; }

.ows-badge { background: #101c30; border: 1px solid #1e2c47; border-radius: 10px; padding: 6px 14px; line-height: 1.1; }
.ows-badge-label { font-size: 10px; letter-spacing: 2px; color: #7a8aa3; font-weight: 700; }
.ows-badge-value { font-size: 13px; font-weight: 600; color: #fff; }

.ows-content { padding: 32px; flex: 1; }

/* CARDS */
.ows-card {
    background: linear-gradient(180deg, #0f1a2d, #0b1524);
    border: 1px solid #1e2c47; border-radius: 16px;
    padding: 22px; box-shadow: 0 4px 22px rgba(0,0,0,.35);
}
.ows-card h3 { font-size: 11px; letter-spacing: 3px; color: #7a8aa3; font-weight: 700; margin-bottom: 12px; }
.ows-card .ows-value { font-size: 32px; font-weight: 700; color: #fff; letter-spacing: 1px; }
.ows-card .ows-hint  { font-size: 12px; color: #7a8aa3; margin-top: 6px; }

.ows-grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: 18px; }
.ows-grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
.ows-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; }
@media (max-width: 1200px) {
    .ows-grid-4 { grid-template-columns: repeat(2, 1fr); }
    .ows-grid-3 { grid-template-columns: 1fr 1fr; }
}
@media (max-width: 800px) {
    .ows-grid-4, .ows-grid-3, .ows-grid-2 { grid-template-columns: 1fr; }
}

/* HERO */
.ows-hero {
    position: relative; border-radius: 20px; padding: 34px; margin-bottom: 24px;
    overflow: hidden;
    background: radial-gradient(circle at 90% 20%, rgba(255,122,0,.20), transparent 60%),
                linear-gradient(135deg, #10192c 0%, #0a1220 100%);
    border: 1px solid #26344f;
}
.ows-hero-label {
    display: inline-block; padding: 5px 12px;
    background: rgba(255,122,0,.15); border: 1px solid rgba(255,122,0,.35);
    color: #ffb069; border-radius: 999px;
    font-size: 11px; letter-spacing: 2px; font-weight: 700; margin-bottom: 14px;
}
.ows-hero h1 { font-size: 30px; font-weight: 800; letter-spacing: 1px; color: #fff; margin-bottom: 6px; }
.ows-hero .ows-hero-sub { color: #b2bccb; margin-bottom: 18px; font-size: 14px; }
.ows-hero-meta { display: flex; gap: 30px; flex-wrap: wrap; margin-top: 20px; }
.ows-hero-meta > div { line-height: 1.2; }
.ows-hero-meta small { color: #7a8aa3; font-size: 11px; letter-spacing: 2px; font-weight: 700; }
.ows-hero-meta strong { display: block; font-size: 18px; font-weight: 700; color: #fff; margin-top: 4px; }
.ows-hero-actions { margin-top: 26px; display: flex; gap: 12px; flex-wrap: wrap; }

/* BOTÕES */
.ows-btn {
    display: inline-block; padding: 10px 18px; border-radius: 10px;
    font-weight: 600; font-size: 14px; cursor: pointer;
    border: 1px solid transparent; transition: all .15s ease;
    text-align: center;
}
.ows-btn-primary { background: linear-gradient(135deg, #ff7a00, #ff3d00); color: #fff; box-shadow: 0 4px 18px rgba(255,120,0,.35); }
.ows-btn-primary:hover { filter: brightness(1.05); }
.ows-btn-ghost { background: transparent; color: #d6dcea; border-color: #26344f; }
.ows-btn-ghost:hover { background: #101c30; color: #fff; }
.ows-btn-danger { background: #b93030; color: #fff; }
.ows-btn-full { display: block; width: 100%; margin-top: 6px; }

/* LAYOUT SEM LOGIN */
.ows-guest { min-height: 100vh; background: #08101e; display: flex; flex-direction: column; }
.ows-guest-topbar {
    display: flex; justify-content: space-between; align-items: center;
    padding: 18px 30px; border-bottom: 1px solid #1a2740;
    background: #0a1322;
}
.ows-guest-brand { font-weight: 800; letter-spacing: 2px; display: flex; align-items: center; gap: 10px; color: #fff; }
.ows-guest-content { padding: 40px; max-width: 1100px; margin: 0 auto; width: 100%; flex: 1; }

/* LANDING */
.ows-landing {
    max-width: 720px; margin: 40px auto; text-align: center;
    background: linear-gradient(180deg, #0f1a2d, #0b1524);
    border: 1px solid #1e2c47; border-radius: 20px;
    padding: 50px 40px;
}
.ows-landing h1 { font-size: 30px; color: #fff; letter-spacing: 1px; }
.ows-landing-sub { color: #b2bccb; font-size: 15px; margin: 12px 0 26px; }

/* AUTH (login / cadastro) */
.ows-auth-wrap { display: flex; justify-content: center; align-items: flex-start; padding: 30px 0; }
.ows-auth-card {
    width: 100%; max-width: 420px;
    background: linear-gradient(180deg, #0f1a2d, #0b1524);
    border: 1px solid #1e2c47; border-radius: 20px;
    padding: 34px;
}
.ows-auth-card h1 { font-size: 24px; color: #fff; margin: 8px 0 4px; letter-spacing: 1px; }
.ows-form { display: flex; flex-direction: column; margin-top: 18px; }
.ows-form label {
    font-size: 11px; letter-spacing: 2px; color: #7a8aa3;
    font-weight: 700; margin-top: 12px; margin-bottom: 6px;
}
.ows-form input {
    background: #0b1524; border: 1px solid #26344f; color: #fff;
    padding: 12px 14px; border-radius: 10px; font-size: 14px;
    outline: none;
}
.ows-form input:focus { border-color: #ff7a00; }
.ows-form button { margin-top: 20px; }
.ows-alert {
    margin-top: 14px; padding: 10px 14px;
    background: rgba(255, 70, 70, .1); border: 1px solid rgba(255, 70, 70, .4);
    color: #ffb0b0; border-radius: 10px; font-size: 13px;
}
.ows-divider {
    display: flex; align-items: center; gap: 10px;
    margin: 20px 0; color: #556277; font-size: 12px;
}
.ows-divider::before, .ows-divider::after {
    content: ''; flex: 1; height: 1px; background: #1e2c47;
}
.ows-auth-footer { text-align: center; margin-top: 20px; font-size: 13px; color: #b2bccb; }

/* Utilitários */
.ows-muted  { color: #7a8aa3; }
.ows-strong { color: #fff; font-weight: 600; }
.ows-orange { color: #ff8a3d; }

===== static/css/tema_v3.css =====
body{margin:0;font-family:Segoe UI,sans-serif;background:#090d14;color:#eaeaea}.shell{display:flex}.sidebar{width:260px;min-height:100vh;background:#0f1622;border-right:1px solid #1e293b}.logo{padding:24px;font-weight:700}.logo span{color:#ff7a00}.sidebar a{display:block;padding:14px 24px;color:#b8c0cc;text-decoration:none}.sidebar a.active,.sidebar a:hover{background:#1a2333;color:#fff}.topbar{display:flex;justify-content:space-between;padding:20px 30px}.hero{margin:20px;padding:30px;border-radius:20px;background:linear-gradient(135deg,#121a26,#1a2330);border:1px solid #263246}.hero h1{margin:0;color:#fff}.countdown{font-size:28px;color:#ff7a00;margin:15px 0}.hero button{background:#ff7a00;color:#fff;border:0;padding:12px 20px;border-radius:10px}.cards{display:grid;grid-template-columns:repeat(4,1fr);gap:20px;padding:20px}.card{background:#121a26;border:1px solid #263246;border-radius:16px;padding:20px}.card span{font-size:32px;font-weight:700;color:#ff7a00}
===== static/css/variaveis.css =====
/* ============================================================
   OPEN WHEEL STRATEGY - Variáveis Globais (Tema)
   ============================================================ */

:root {
    /* Tipografia */
    --font-family: 'Segoe UI', 'Inter', Tahoma, sans-serif;
    --font-mono: 'Courier New', Courier, monospace;

    /* Cores de Fundo (Backgrounds) */
    --bg-body: #08101e;
    --bg-sidebar: #0b1524;
    --bg-topbar: #0a1322;
    --bg-surface: #101c30;
    --bg-card-start: #0f1a2d;
    --bg-card-end: #0b1524;
    --bg-hover: #0f1c30;
    
    /* Cores de Texto */
    --text-white: #ffffff;
    --text-main: #eef2f8;
    --text-muted: #b2bccb;
    --text-label: #7a8aa3;
    --text-dark: #5a6a83;

    /* Acentos e Cores de Marca (Primary = Laranja) */
    --color-primary: #ff7a00;
    --color-primary-dark: #ff3d00;
    --color-primary-light: #ff8a3d;
    --color-primary-faded: rgba(255, 122, 0, 0.15);
    --color-primary-border: rgba(255, 122, 0, 0.35);
    
    /* Cores de Feedback (Status) */
    --color-danger: #b93030;
    --color-danger-light: #ffb0b0;
    --color-danger-bg: rgba(255, 70, 70, 0.1);
    --color-danger-border: rgba(255, 70, 70, 0.4);
    
    --color-success: #2ecc71;
    --color-success-light: #8fe2a0;
    --color-success-bg: rgba(46, 125, 50, 0.12);
    --color-success-border: rgba(46, 125, 50, 0.4);

    --color-warning: #f1c40f;
    --color-warning-bg: rgba(241, 196, 15, 0.1);
    --color-warning-border: rgba(241, 196, 15, 0.4);

    --color-info: #3498db;
    --color-info-bg: rgba(52, 152, 219, 0.1);
    --color-info-border: rgba(52, 152, 219, 0.4);

    /* Bordas e Divisores */
    --border-light: #26344f;
    --border-main: #1e2c47;
    --border-dark: #1a2740;

    /* Sombras e Efeitos */
    --shadow-card: 0 4px 22px rgba(0, 0, 0, 0.35);
    --shadow-btn-primary: 0 4px 18px rgba(255, 120, 0, 0.35);
    --shadow-logo: 0 0 20px rgba(255, 120, 0, 0.35);

    /* Espaçamentos Globais */
    --spacing-sm: 8px;
    --spacing-md: 16px;
    --spacing-lg: 24px;
    --spacing-xl: 32px;
    
    /* Arredondamentos */
    --radius-sm: 4px;
    --radius-md: 10px;
    --radius-lg: 16px;
    --radius-xl: 20px;
}
